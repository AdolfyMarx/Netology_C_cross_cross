#include <compare>
#include <iostream>
#include <cmath>

int main() {
    bool flag = 1.0 / sqrt(3) == sqrt(3) / 3;
    std::cout << "ABOBA  " << flag << std::endl;
    unsigned char a = 250;
    unsigned char b = 150;
    unsigned char res =  a + b;

    std::cout << "AMOGUS  " << res << std::endl;
    return 0;
}
